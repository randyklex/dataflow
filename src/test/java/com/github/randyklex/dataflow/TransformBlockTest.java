package com.github.randyklex.dataflow;

import java.util.function.Function;

import static org.junit.Assert.assertEquals;

public class TransformBlockTest {

//    public void testToString(Function<String, IDataflowBlock> blockFactory) {
//        IDataflowBlock block;
//
//        block = blockFactory.apply(null);
//        assertEquals(String.format("{} Id={}", block.getType().Name, block.getCompletion().Id),
//                block.toString());
//    }

//    public void testOfferMessage() {
//
//    }

    public void testPost() {
        for(boolean bounded : DataflowTestHelpers.booleanValues) {
          for(TransformBlock<>
        }
    }
}
